# Target API to proxy requests to
TARGET_URL=https://api.example.com

# Port for the proxy server
PORT=3000

# Set to true to serve all requests from .api-mirror/ files (no live backend needed)
MIRROR_MODE=false

# Add artificial latency to every response (milliseconds)
LATENCY=0
